# AKShare is keyless, but a persistent cache dir helps avoid re-fetch.
DATA_CACHE_DIR=./data_cache

# Optional: LightGBM threads (0 = all cores)
LGBM_NUM_THREADS=0

# Optional: Streamlit port for streamlit_app/app.py
STREAMLIT_SERVER_PORT=8501
